#include <bits/stdc++.h>
using namespace std;
void sortvec(vector<int>&A)
{
    for(int i=0;i<A.size();i++)
    {
        for(int j=i+1;j<A.size();j++)
        {
            if(A[j]<A[i])
            swap(A[j],A[i]);

        }

    }
    for (int i = 0; i < A.size(); i++)
    {
        cout<<A[i]<<endl;
    }
    
  
}


int main()
{
vector<int> A ={ 67,34,10,12,99};
sortvec(A);
return 0;
}
